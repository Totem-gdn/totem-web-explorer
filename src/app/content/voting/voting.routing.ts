import { Route } from "@angular/router";
import { VotingComponent } from "./voting.component";

export const VotingRoutes: Route[] = [
    {
        path: '',
        component: VotingComponent,
    }
]
